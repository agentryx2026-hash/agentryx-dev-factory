# factory-dashboard-snapshot/

**Temporary snapshot** of files modified in `~/Projects/pixel-factory-ui/`
(currently outside this mono-repo). Will be removed when Phase 1.5 physically
moves pixel-factory-ui into `agentryx-factory/factory-dashboard/`.

Contents are the Phase 2.5-C UI additions:

- `src/components/AdminKeys.tsx` — the Key Console page (new)
- `src/components/Sidebar.tsx`   — added "🔑 API Keys" nav item
- `src/App.tsx`                  — added 'admin-keys' page case

Same pattern as `cognitive-engine-snapshot/`.
